<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gm.rtonumbermatcher">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".app.RtoApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/AppTheme.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme" />

        <!-- GmArch Configuration -->
        <meta-data
            android:name="com.gm.rtonumbermatcher.app.RepositoryConfiguration"
            android:value="ConfigRepository" />
        <meta-data
            android:name="com.gm.rtonumbermatcher.app.LifecycleConfiguration"
            android:value="ConfigLifecycle" />
        <meta-data
            android:name="com.gm.rtonumbermatcher.app.GmConfiguration"
            android:value="ConfigGm" />


    </application>

</manifest>